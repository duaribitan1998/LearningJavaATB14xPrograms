package ex_002_JavaBasics;

public class Enum {
    public static void main(String[] args) {

        // Enum can be used as Identifiers because of case Sensitive//
        // but we can't use "enum"  because of predefined keyword //
        System.out.println("Enum can be used as Identifiers since its a case sensitive");
    }
}
